# ShowRun Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ─── LLM Provider ───────────────────────────────────────────────
# Which provider to use: "anthropic" or "openai"
# Auto-detected if omitted (Anthropic if ANTHROPIC_API_KEY is set, otherwise OpenAI)
# LLM_PROVIDER=anthropic

# ─── Anthropic ──────────────────────────────────────────────────
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# ─── OpenAI ─────────────────────────────────────────────────────
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-2024-08-06
# OPENAI_BASE_URL=https://api.openai.com/v1

# ─── Agent Behavior ────────────────────────────────────────────
# Max consecutive browser-only rounds before the agent must use the editor (0 = no limit)
# AGENT_MAX_BROWSER_ROUNDS=0

# ─── Custom System Prompts ──────────────────────────────────────
# Override the default system prompts (set a string directly or point to a file)
# TEACH_CHAT_SYSTEM_PROMPT=
# EXPLORATION_AGENT_PROMPT_PATH=./EXPLORATION_AGENT_SYSTEM_PROMPT.md
